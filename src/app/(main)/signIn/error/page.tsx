import SignButton from "@/components/SignButton";

const SignError = () => {
  return (
    <section>
      <p>오류가 발생했습니다. 다시 로그인 해주세요.</p>
      <SignButton />
    </section>
  );
};

export default SignError;
